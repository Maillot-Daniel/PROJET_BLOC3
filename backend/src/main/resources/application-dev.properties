# ============================================
# DEVELOPMENT PROFILE - Configuration spécifique
# ============================================

# Database Development Settings
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# Logging Development Settings
logging.level.com.olympics.tickets=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG

# Development Features
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# H2 Console (optionnel pour les tests)
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# Development Security (plus permissive)
server.servlet.session.timeout=30m

# Development CORS (plus large)
app.cors.allowed-origins=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000

# Development Stripe (mode test)
stripe.mode=test

# ============================================
# MAILTRAP CONFIGURATION - Développement local
# ============================================

# Configuration Mailtrap RÉELLE
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=587
spring.mail.username=d0c004224e85f3
spring.mail.password=3105c1ce65a126
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ? CRITIQUE : DÉSACTIVER la validation pour le développement
spring.mail.test-connection=false

# Logs détaillés emails
spring.mail.properties.mail.debug=true
logging.level.org.springframework.mail=DEBUG

# URLs frontend développement
frontend.base.url=http://localhost:3000
frontend.success-url=http://localhost:3000/success
frontend.cancel-url=http://localhost:3000/cart

# ============================================
# FEATURE TOGGLES - Développement
# ============================================

# Activer/désactiver des features
app.features.email-enabled=true
app.features.qr-code-enabled=true
app.features.stripe-test-mode=true

# Paramètres développement
app.debug.mode=true
app.demo.data.enabled=false