# ============================================
# MAIN CONFIGURATION - Common settings
# ============================================

# Application
spring.application.name=backend

# Active le profil dev par dï¿½faut
spring.profiles.active=dev

# Server
server.port=8080
server.address=0.0.0.0

# Database (PRODUCTION/DEV COMMON)
spring.datasource.url=jdbc:mysql://mysql-olympicstickets.alwaysdata.net:3306/olympicstickets_db?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=433294
spring.datasource.password=dansunsationdb
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA (COMMON)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false  # Overridden in dev to true

# Hikari Connection Pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000

# Stripe (COMMON)
stripe.secret.key=sk_test_xxxxxxxxxxxxxxxxxx
stripe.public.key=pk_test_xxxxxxxxxxxxxxxxxx
stripe.webhook.secret=whsec_xxxxxxxxxxxxxxxxxx 


# JWT (COMMON)
jwt.secret=your-super-secure-jwt-secret-key-minimum-32-characters-long-123456789

# Frontend (COMMON)
frontend.base.url=http://localhost:3000
frontend.success-url=http://localhost:3000/success
frontend.cancel-url=http://localhost:3000/cancel

# Email (COMMON - dï¿½sactivï¿½ temporairement)
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=your-email@gmail.com
# spring.mail.password=your-app-password
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# Logging (COMMON - overridden in dev)
logging.level.com.olympics.tickets=INFO

# Sécurité HMAC pour les tickets
app.hmac.secret=YourSuperSecureHMACSecretKeyForTickets123!

# Clé secrète pour la signature HMAC (À CHANGER en production)
ticket.signature.secret=your-super-secure-secret-key-change-in-production-2024

# Dossier de sortie pour les QR codes
app.qr-code.output-dir=/tmp/olympics-qrcodes

# Nettoyage automatique des QR codes après 7 jours
app.qr-code.cleanup-days=7